name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: macos-14

    steps:
      - uses: actions/checkout@v4

      - name: Resolve Swift package dependencies
        run: swift package resolve

      - name: Build
        run: swift build
